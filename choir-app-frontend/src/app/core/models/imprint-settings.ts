export interface ImprintSettings {
  name: string;
  street: string;
  postalCode: string;
  city: string;
  country: string;
  phone?: string;
  email: string;
  responsibleName?: string;
  isComplete: boolean;
}
